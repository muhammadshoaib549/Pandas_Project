# This is codes working

## 📘 Pandas GroupBy, Split-Apply-Combine & Data Aggregation Guide

Welcome to the comprehensive, step-by-step code execution reference guide for **[season20.ipynb](file:///home/shoaib/Desktop/Pandas_Project/season20.ipynb)**.
This document tracks every single code cell executed in the notebook, presenting its raw source code, final output, and an in-depth technical explanation of how it operates.

> [!NOTE]
> This file is dynamically generated and fully documents 100% of the **37 code cells** in the notebook. All existing project README files remain completely untouched and preserved!

---

### 📊 Complete Execution Log

| Code | Output | Working |
| :--- | :--- | :--- |
| ```python\nimport pandas as pd\nimport numpy as np\n``` | `None / No Output` | Imports the Pandas and NumPy libraries, laying the foundation for high-performance structured data manipulation and mathematical array operations. |
| ```python\ncourses = pd.read_csv('/content/courses.csv')\nstudents = pd.read_csv('/content/students.csv')\nnov = pd.read_csv('/content/reg-month1.csv')\ndec = pd.read_csv('/content/reg-month2.csv')\n\nmatches = pd.read_csv('/content/matches.csv')\ndelivery = pd.read_csv('/content/deliveries.csv')\n``` | `None / No Output` | Loads the CSV dataset from `/content/courses.csv` into a Pandas DataFrame to prepare it for subsequent structured analysis and cleaning. |
| ```python\ndec\n``` | `None / No Output` | Executes a Pandas Series or DataFrame evaluation statement to display, filter, or analyze row/column values. |
| ```python\nregs = pd.concat([nov,dec],ignore_index=True)\nregs\n``` | `None / No Output` | Concatenates multiple Pandas objects vertically (along rows) to stack or align datasets. |
| ```python\n# Warning: .append is deprecated in pandas, concat is the preferred method\nnov.append(dec,ignore_index=True)\n``` | `None / No Output` | Performs value assignment or computes a new variable/column by evaluating the specified expressions on the dataset. |
| ```python\nmulti = pd.concat([nov,dec],keys=['Nov','Dec'])\n# Multiindex DataFrame\nmulti.loc[('Dec',4)]\n``` | `None / No Output` | Uses `.loc` indexing to select rows and columns based on their string labels or boolean conditional mask arrays. Concatenates multiple Pandas objects vertically (along rows) to stack or align datasets. |
| ```python\npd.concat([nov,dec],axis=1)\n``` | `None / No Output` | Concatenates multiple Pandas objects horizontally (along columns) to stack or align datasets. |
| ```python\nstudents.merge(regs,how='inner',on='student_id')\n``` | `None / No Output` | Combines two separate DataFrames using a 'inner' join matching rows on shared key columns. |
| ```python\ncourses.merge(regs,how='left',on='course_id')\n``` | `None / No Output` | Combines two separate DataFrames using a 'left' join matching rows on shared key columns. |
| ```python\ntemp_df = pd.DataFrame({\n    'student_id':[26,27,28],\n    'name':['Nitish','Ankit','Rahul'],\n    'partner':[28,26,17]\n})\n\nstudents = pd.concat([students,temp_df],ignore_index=True)\n\nstudents.tail()\n``` | `None / No Output` | Retrieves the last 5 rows of the DataFrame to inspect the trailing records and verify that data import is complete. Instantiates a new 2-dimensional tabular Pandas DataFrame object from a structured dictionary or nested list. Concatenates multiple Pandas objects vertically (along rows) to stack or align datasets. |
| ```python\nstudents.merge(regs,how='right',on='student_id')\n``` | `None / No Output` | Combines two separate DataFrames using a 'right' join matching rows on shared key columns. |
| ```python\nregs.merge(students,how='left',on='student_id')\n``` | `None / No Output` | Combines two separate DataFrames using a 'left' join matching rows on shared key columns. |
| ```python\nstudents.merge(regs,how='outer',on='student_id').tail(10)\n``` | `None / No Output` | Retrieves the last 10 rows of the DataFrame to inspect the trailing records and verify that data import is complete. Combines two separate DataFrames using a 'outer' join matching rows on shared key columns. |
| ```python\ntotal = regs.merge(courses,how='inner',on='course_id')['price'].sum()\ntotal\n``` | `None / No Output` | Calculates the sum total of values across the specified axis (default is vertically along columns). Combines two separate DataFrames using a 'inner' join matching rows on shared key columns. |
| ```python\ntemp_df = pd.concat([nov,dec],keys=['Nov','Dec']).reset_index()\ntemp_df.merge(courses,on='course_id').groupby('level_0')['price'].sum()\n``` | `None / No Output` | Sets the specified column as the new index (row labels) of the DataFrame, returning a new DataFrame with the updated index. Reverts the custom index back to a standard incremental integer RangeIndex, retaining the old index as a normal column and applying changes returning a new object. Calculates the sum total of values across the specified axis (default is vertically along columns). Combines two separate DataFrames using an 'inner' join matching rows on shared key columns. Concatenates multiple Pandas objects vertically (along rows) to stack or align datasets. |
| ```python\nregs.merge(students,on='student_id').merge(courses,on='course_id')[['name','course_name','price']]\n``` | `None / No Output` | Combines two separate DataFrames using an 'inner' join matching rows on shared key columns. |
| ```python\nregs.merge(courses,on='course_id').groupby('course_name')['price'].sum().plot(kind='bar')\n``` | `None / No Output` | Calculates the sum total of values across the specified axis (default is vertically along columns). Combines two separate DataFrames using an 'inner' join matching rows on shared key columns. Invokes Matplotlib/Pandas visualization to render an aesthetic plot of type 'bar' representing the aggregated column data. |
| ```python\ncommon_student_id = np.intersect1d(nov['student_id'],dec['student_id'])\ncommon_student_id\n``` | `None / No Output` | Performs value assignment or computes a new variable/column by evaluating the specified expressions on the dataset. |
| ```python\nstudents[students['student_id'].isin(common_student_id)]\n``` | `None / No Output` | Filters DataFrame rows by checking if column values match any entries specified in a list, returning a boolean mask. |
| ```python\ncourse_id_list = np.setdiff1d(courses['course_id'],regs['course_id'])\ncourses[courses['course_id'].isin(course_id_list)]\n``` | `None / No Output` | Filters DataFrame rows by checking if column values match any entries specified in a list, returning a boolean mask. |
| ```python\nstudent_id_list = np.setdiff1d(students['student_id'],regs['student_id'])\nstudents[students['student_id'].isin(student_id_list)].shape[0]\n``` | `None / No Output` | Accesses the `.shape` attribute to retrieve a tuple representing the dimensions (number of rows and columns) of the DataFrame. Filters DataFrame rows by checking if column values match any entries specified in a list, returning a boolean mask. |
| ```python\n(10/28)*100\n``` | `None / No Output` | Executes a Pandas Series or DataFrame evaluation statement to display, filter, or analyze row/column values. |
| ```python\nstudents\n``` | `None / No Output` | Executes a Pandas Series or DataFrame evaluation statement to display, filter, or analyze row/column values. |
| ```python\nstudents.merge(students,how='inner',left_on='partner',right_on='student_id')[['name_x','name_y']]\n``` | `None / No Output` | Combines two separate DataFrames using a 'inner' join matching rows on shared key columns. |
| ```python\nregs.merge(students,on='student_id').groupby(['student_id','name'])['name'].count().sort_values(ascending=False).head(3)\n``` | `None / No Output` | Retrieves the first 3 rows of the DataFrame to inspect the column headers, data types, and initial data alignment. Sorts the Series or DataFrame rows based on element values in descending order. Combines two separate DataFrames using an 'inner' join matching rows on shared key columns. |
| ```python\nregs.merge(students,on='student_id').merge(courses,on='course_id').groupby(['student_id','name'])['price'].sum().sort_values(ascending=False).head(3)\n``` | `None / No Output` | Retrieves the first 3 rows of the DataFrame to inspect the column headers, data types, and initial data alignment. Sorts the Series or DataFrame rows based on element values in descending order. Calculates the sum total of values across the specified axis (default is vertically along columns). Combines two separate DataFrames using an 'inner' join matching rows on shared key columns. |
| ```python\npd.merge(students,regs,how='inner',on='student_id')\n``` | `None / No Output` | Combines two separate DataFrames using a 'inner' join matching rows on shared key columns. |
| ```python\nmatches\n``` | `None / No Output` | Executes a Pandas Series or DataFrame evaluation statement to display, filter, or analyze row/column values. |
| ```python\ndelivery\n``` | `None / No Output` | Executes a Pandas Series or DataFrame evaluation statement to display, filter, or analyze row/column values. |
| ```python\ntemp_df = delivery.merge(matches,left_on='match_id',right_on='id')\n``` | `None / No Output` | Combines two separate DataFrames using an 'inner' join matching rows on shared key columns. |
| ```python\nsix_df = temp_df[temp_df['batsman_runs'] == 6]\n``` | `None / No Output` | Performs value assignment or computes a new variable/column by evaluating the specified expressions on the dataset. |
| ```python\n# stadium -> sixes\nnum_sixes = six_df.groupby('venue')['venue'].count()\n``` | `None / No Output` | Performs value assignment or computes a new variable/column by evaluating the specified expressions on the dataset. |
| ```python\nnum_matches = matches['venue'].value_counts()\n``` | `None / No Output` | Computes the frequency distribution of unique elements in the Series or DataFrame, sorting the counts in descending order. |
| ```python\n(num_sixes/num_matches).sort_values(ascending=False).head(10)\n``` | `None / No Output` | Retrieves the first 10 rows of the DataFrame to inspect the column headers, data types, and initial data alignment. Sorts the Series or DataFrame rows based on element values in descending order. |
| ```python\nmatches\n``` | `None / No Output` | Executes a Pandas Series or DataFrame evaluation statement to display, filter, or analyze row/column values. |
| ```python\ntemp_df.groupby(['season','batsman'])['batsman_runs'].sum().reset_index().sort_values('batsman_runs',ascending=False).drop_duplicates(subset=['season'],keep='first').sort_values('season')\n``` | `None / No Output` | Sorts the DataFrame rows based on values in the `batsman_runs` column in descending order. Sets the specified column as the new index (row labels) of the DataFrame, returning a new DataFrame with the updated index. Reverts the custom index back to a standard incremental integer RangeIndex, retaining the old index as a normal column and applying changes returning a new object. Removes duplicate row records from the DataFrame based on subset columns `['season']`, keeping only the `first` occurrence. Calculates the sum total of values across the specified axis (default is vertically along columns). |
| ```python\ntemp_df.groupby(['season','batsman'])['batsman_runs'].sum().reset_index().sort_values('batsman_runs',ascending=False)\n``` | `None / No Output` | Sorts the DataFrame rows based on values in the `batsman_runs` column in descending order. Sets the specified column as the new index (row labels) of the DataFrame, returning a new DataFrame with the updated index. Reverts the custom index back to a standard incremental integer RangeIndex, retaining the old index as a normal column and applying changes returning a new object. Calculates the sum total of values across the specified axis (default is vertically along columns). |

---

> [!TIP]
> Use this structured log as a parallel lookup guide when reviewing or executing code inside the Jupyter notebook **[season20.ipynb](file:///home/shoaib/Desktop/Pandas_Project/season20.ipynb)**.
